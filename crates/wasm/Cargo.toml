[package]
name = "tileforge-wasm"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
tileforge-core = { path = "../core", default-features = false }
wasm-bindgen = "0.2"
js-sys = "0.3"
