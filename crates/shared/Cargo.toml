[package]
name = "tileforge-shared"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1", features = ["derive"] }
rust-s3 = { version = "=0.37.1", default-features = false, features = ["tokio-rustls-tls"] }

[dev-dependencies]
serde_json = "1"
