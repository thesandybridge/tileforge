# Projections

TileForge supports three projection modes for tile generation.

## Flat Projection

Flat projection treats the source image as a simple 2D plane. The image is divided into tiles without any geographic distortion.

```bash
tileforge input.png -o tiles --projection flat
```

**Use cases**:
- Game maps and virtual worlds
- Architectural floor plans
- Art and illustrations
- Any non-geographic image

<CalloutCard title="Default Projection" variant="info">
  Flat projection is the default. Use this unless your source image is a geographic map or you want an isometric view.
</CalloutCard>

## Mercator Projection

Mercator projection applies the Web Mercator (EPSG:3857) transformation used by web mapping services like Google Maps, OpenStreetMap, and Mapbox.

```bash
tileforge input.png -o tiles --projection mercator
```

**Use cases**:
- Geographic maps and satellite imagery
- Integration with existing map tile services
- Overlaying custom data on standard web maps

## Isometric Projection

Isometric projection transforms a top-down image into a 2.5D isometric perspective, creating a pseudo-3D effect.

```bash
tileforge input.png -o tiles --projection isometric
```

**Use cases**:
- Game world maps (city builders, strategy games)
- Architectural visualizations
- Technical diagrams with depth

<CalloutCard title="Streaming Limitation" variant="warning">
  Isometric projection requires access to the entire image for the transformation, so it uses the naive (full-image) processing mode rather than row-by-row streaming. Very large images may need more memory.
</CalloutCard>

## How It Works

### Flat
The image is simply subdivided at each zoom level. At zoom 0, the entire image is one 256x256 tile. At zoom 1, it becomes 2x2 tiles, and so on.

### Mercator
The image is reprojected into the Web Mercator coordinate system before tiling. This ensures tiles align correctly when overlaid on standard web maps.

### Isometric
The image is rotated 45 degrees and scaled vertically by ~0.5x to create the isometric perspective, then tiled normally.

## Choosing a Projection

| Scenario | Projection |
|----------|-----------|
| Game map / floor plan | Flat |
| Geographic map | Mercator |
| Artwork / illustration | Flat |
| Satellite overlay | Mercator |
| Technical diagram | Flat |
| City builder game map | Isometric |
| 2.5D visualization | Isometric |
